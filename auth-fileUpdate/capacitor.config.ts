import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'auth-fileUpdate',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
